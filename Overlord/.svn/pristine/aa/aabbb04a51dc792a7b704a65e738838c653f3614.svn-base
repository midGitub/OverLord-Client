using UnityEngine;
using System.Collections;

public class AreaBullet : Bullet
{
    public override void ReachedVit()
    {
        //Soldier vitScript = vitSoldier.GetComponent<Soldier>();
        //foreach (GameObject vitfriend in vitScript.belongCityManager.FindFriendsInRange(vitScript, 5f))
        //{
        //    vitfriend.GetComponent<Soldier>().ReceiveDamage(damage / 2);
        //}

        //vitSoldier.GetComponent<Soldier>().ReceiveDamage(damage);
        //Destroy(gameObject);
    }
}
